on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test
        uses: PenguinCabinet/tyranoscript-packager-action@v0.1.2
        with:
          workingdirectory: "test"
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}